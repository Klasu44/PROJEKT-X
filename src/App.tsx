import {useMemo,useState} from 'react';
import {regions,headlines} from './game/world';import {npcs,blocLabels,NPC} from './game/people';import {initialState,stateActions,stateGroups,applyAction,StateSnapshot} from './game/state';import {initialPolitics,offers,acceptOffer,PoliticalState} from './game/politics';import {nextEvent,applyEventChoice,GameEvent} from './game/events';import {campaign1994} from './game/campaign1994';
type Tab='biurko'|'kampania'|'kryzysy'|'państwo'|'polityka'|'mapa'|'ludzie'|'archiwum';
export default function App(){const [tab,setTab]=useState<Tab>('biurko'),[selected,setSelected]=useState(regions[0]),[person,setPerson]=useState<NPC>(npcs[0]),[filter,setFilter]=useState('wszyscy'),[state,setState]=useState<StateSnapshot>(initialState),[politics,setPolitics]=useState<PoliticalState>(initialPolitics),[log,setLog]=useState<string[]>([]),[reply,setReply]=useState(''),[seen,setSeen]=useState<string[]>([]),[eventLog,setEventLog]=useState<string[]>([]),[chapter,setChapter]=useState(0),[flags,setFlags]=useState<string[]>([]),[chapterLog,setChapterLog]=useState<string[]>([]);const visible=useMemo(()=>filter==='wszyscy'?npcs:npcs.filter(n=>n.bloc===filter),[filter]),current=nextEvent(state,politics,seen),scene=campaign1994[chapter],regionState=state.regions[selected.id];
const act=(id:string)=>{const a=stateActions.find(x=>x.id===id)!;setState(s=>applyAction(s,a));setLog(l=>[a.label,...l].slice(0,6));};const deal=(id:string)=>{const o=offers.find(x=>x.id===id)!;if(politics.memories.some(m=>m.id===o.memory.id))return;setPolitics(p=>acceptOffer(p,o,state.day));setReply(o.response);setPerson(npcs.find(n=>n.id===o.personId)!);};const choose=(e:GameEvent,id:string)=>{const c=e.choices.find(x=>x.id===id)!;if(c.memoryRequired&&!politics.memories.some(m=>m.id===c.memoryRequired))return;const r=applyEventChoice(state,politics,e,c);setState(r.state);setPolitics(r.politics);setSeen(x=>[...x,e.id,...(c.followup?['queue:'+c.followup]:[])]);setEventLog(x=>[`${e.title} — ${c.label}`,...x].slice(0,8));};const chapterChoice=(id:string)=>{if(!scene)return;const c=scene.choices.find(x=>x.id===id)!;if(c.requires&&!flags.includes(c.requires))return;setState(s=>{const n={...s};Object.entries(c.effects??{}).forEach(([k,v])=>{if(typeof (n as any)[k]==='number')(n as any)[k]=Number(((n as any)[k]+v).toFixed(1));});n.day+=7;return n});setFlags(f=>[...f,...(c.flags??[])]);setChapterLog(l=>[`${scene.month}: ${scene.title} — ${c.label}. ${c.result}`,...l]);setChapter(x=>x+1);};
return <div className="shell"><header><div><b>INTERREGNUM</b><span> REPUBLIKA VELIGRADU</span></div><div className="date">1994 · TYDZIEŃ {Math.ceil(state.day/7)}</div></header><nav>{(['biurko','kampania','kryzysy','państwo','polityka','mapa','ludzie','archiwum'] as Tab[]).map(x=><button key={x} className={tab===x?'active':''} onClick={()=>setTab(x)}>{x.toUpperCase()}{x==='kryzysy'&&current?' · !':''}</button>)}</nav>
{tab==='biurko'&&<main className="desk"><section className="brief"><div className="stamp">ŚCIŚLE TAJNE</div><p className="kicker">KOMITET STABILIZACJI PAŃSTWA · BIULETYN 001</p><h1>Państwo działa.<br/>Na razie.</h1><p>Republika Veligradu weszła w 1994 rok bez stabilnej większości, z konfliktem prezydenta i premiera, uciekającymi rezerwami, Draviną na granicy strajku i Karskiem testującym granice autonomii.</p><p>Parlament powołał <strong>Komitet Stabilizacji Państwa</strong>. Miał być sekretariatem technicznym. Bardzo szybko przestał nim być.</p><div className="signature">SEKRETARIAT KSP / EGZ. 1</div></section><aside><h3>STAN PAŃSTWA</h3><div className="metric"><span>Rezerwy</span><b>{state.reserves} mln ₣</b></div><div className="metric"><span>Inflacja</span><b>{state.inflation}%</b></div><div className="metric"><span>Kapitał polityczny</span><b>{politics.capital}</b></div><div className="metric"><span>Rozdział kampanii</span><b>{Math.min(chapter+1,campaign1994.length)}/{campaign1994.length}</b></div>{current&&<><h3>PILNE</h3><p className="warning">{current.title}</p></>}</aside></main>}
{tab==='kampania'&&<main className="campaign"><section><p className="kicker">KAMPANIA 1994–1998 / ROZDZIAŁ I</p><h1>Pierwsze półrocze</h1>{scene?<article className="chapter"><div className="chaptermonth">{scene.month}</div><p className="kicker">{scene.source}</p><h2>{scene.title}</h2><p>{scene.body}</p><div className="chapterchoices">{scene.choices.map(c=><button key={c.id} disabled={!!c.requires&&!flags.includes(c.requires)} onClick={()=>chapterChoice(c.id)}><b>{c.label}</b><span>{c.result}</span></button>)}</div></article>:<div className="chapter finished"><h2>Rozdział I zakończony.</h2><p>Masz za sobą pierwsze sześć miesięcy Interregnum. Stan państwa i podjęte ścieżki zostają zachowane jako fundament dalszej kampanii.</p></div>}</section><aside><p className="kicker">KRONIKA 1994</p>{chapterLog.length?chapterLog.map((x,i)=><p className="chronicle" key={i}>{x}</p>):<p className="muted">Jeszcze nic nie zostało zapisane.</p>}<h3>TRWAŁE ŚCIEŻKI</h3>{flags.length?flags.map(x=><span className="flag" key={x}>{x.replaceAll('_',' ').toUpperCase()}</span>):<p className="muted">Brak.</p>}</aside></main>}
{tab==='kryzysy'&&<main className="crisis"><section><p className="kicker">SYSTEM ZDARZEŃ / PRIORYTET OPERACYJNY</p><h1>Kryzysy</h1>{current?<article className="eventpaper"><div className="eventhead"><span>{current.source}</span><b>PRIORYTET {current.priority}</b></div><h2>{current.title}</h2><p>{current.body}</p><div className="choices">{current.choices.map(c=>{const locked=!!c.memoryRequired&&!politics.memories.some(m=>m.id===c.memoryRequired);return <button key={c.id} disabled={locked} onClick={()=>choose(current,c.id)}><b>{c.label}</b><span>{c.text}</span>{locked&&<em>WYMAGA WCZEŚNIEJSZEGO KONTAKTU</em>}</button>})}</div></article>:<div className="quiet"><h2>Brak spraw wymagających natychmiastowej decyzji.</h2></div>}</section><aside><p className="kicker">KRONIKA DECYZJI</p>{eventLog.map((x,i)=><p className="log" key={i}>{x}</p>)}</aside></main>}
{tab==='państwo'&&<main className="stateview"><section><p className="kicker">CENTRUM OPERACYJNE KSP · DZIEŃ {state.day}</p><h1>Stan państwa</h1><div className="stategrid">{stateGroups.map(g=><div className="statebox" key={g.name}><h3>{g.name}</h3>{g.keys.map(([key,label,unit])=><div className="statrow" key={key}><span>{label}</span><b>{(state as any)[key]} {unit}</b></div>)}</div>)}</div><h2>Decyzje wykonawcze</h2><div className="actions">{stateActions.map(a=><button key={a.id} onClick={()=>act(a.id)}><b>{a.label}</b><span>{a.detail}</span></button>)}</div></section><aside>{log.map((x,i)=><p className="log" key={i}>{x}</p>)}</aside></main>}
{tab==='polityka'&&<main className="politics"><section><p className="kicker">GABINET SEKRETARZA</p><h1>Polityka</h1><div className="politicaltop"><div><b>{politics.capital}</b><small>KAPITAŁ</small></div><div><b>{politics.mandates}</b><small>MANDATY / 240</small></div><div><b>{politics.memories.length}</b><small>ZOBOWIĄZANIA</small></div></div><div className="dealgrid">{offers.map(o=>{const n=npcs.find(x=>x.id===o.personId)!,done=politics.memories.some(m=>m.id===o.memory.id);return <article className={done?'deal done':'deal'} key={o.id}><p className="kicker">{n.name}</p><h3>{o.title}</h3><p>{o.ask}</p><div className="price">{o.price}</div><button disabled={done} onClick={()=>deal(o.id)}>{done?'UZGODNIONO':'PRZYJMIJ WARUNKI'}</button></article>})}</div></section><aside>{reply&&<div className="reply">{reply}</div>}{politics.memories.map(m=><div className="memory" key={m.id}><b>{m.kind}</b><span>{m.label}</span></div>)}</aside></main>}
{tab==='mapa'&&<main className="mapview"><section><h1>Republika Veligradu</h1><div className="regions">{regions.map(r=><button key={r.id} onClick={()=>setSelected(r)} className={selected.id===r.id?'selected':''}><b>{r.name}</b><small>{r.subtitle}</small></button>)}</div></section><aside><h2>{selected.name}</h2><p>{selected.population}</p><div className="bar"><i style={{width:(regionState?.stability??selected.stability)+'%'}}/></div><small>STABILNOŚĆ {regionState?.stability??selected.stability}</small><p>{selected.note}</p></aside></main>}
{tab==='ludzie'&&<main className="people"><section><h1>Ośrodki siły</h1><div className="filters"><button onClick={()=>setFilter('wszyscy')}>WSZYSCY</button>{Object.entries(blocLabels).map(([k,v])=><button key={k} onClick={()=>setFilter(k)}>{v}</button>)}</div><div className="cards">{visible.map(n=><button key={n.id} className="personcard" onClick={()=>setPerson(n)}><span className="initials">{n.name.split(' ').map(x=>x[0]).join('')}</span><div><b>{n.name}</b><small>{n.role}</small></div><strong>{n.influence}</strong></button>)}</div></section><aside className="dossier"><h2>{person.name}</h2><p>{person.role}</p><p className="quote">„{person.publicFace}”</p><dl><dt>DĄŻY DO</dt><dd>{person.drive}</dd><dt>OBAWIA SIĘ</dt><dd>{person.fear}</dd><dt>DŹWIGNIA</dt><dd>{person.leverage}</dd></dl></aside></main>}
{tab==='archiwum'&&<main className="archive"><h1>1945—1994</h1>{[['1945','Powstaje Republika Ludowa Veligradu.'],['1974','Układ Karski.'],['1989','Koniec monopolu partii.'],['1991','Konstytucja kompromisu.'],['1994','INTERREGNUM.']].map(([y,t])=><article key={y}><b>{y}</b><p>{t}</p></article>)}</main>}<footer>TURA 6 BUILD · CAMPAIGN CONTENT · v0.6.0</footer></div>}
